# FU-22 (B) 増分② フィルタ 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ChannelBrowser の検索フィルタを階層ツリーで復旧する（増分① で proxy 撤去に伴い skip 中）。prod 264k で per-keystroke フリーズを起こさない。

**Architecture:** フィルタは VM 側（`tree_groups()` が `fl in leaf名` でリーフを絞り base で再グルーピング）。支配コストの O(264k) スキャン ~170ms を **debounce（~200ms singleShot QTimer）** で入力停止後 1 回に集約。設計 [spec](../specs/2026-07-13-fu22b-channel-tree-virtualization-design.md)「増分② フィルタ実測」。

**Tech Stack:** PySide6/pyqtgraph, MVVM(Observable), pytest-qt。

## Global Constraints

- core は Qt 非依存維持。`ChannelBrowserVM` の変更は非 Qt（`tree_groups`）。debounce は view（Qt）。
- 品質ゲート: `uv run pytest` / `uv run ruff check` / `uv run ruff format --check` / `uv run mypy src/` 全通過。
- Python コメント/文字列に全角約物 `()：+=` 禁止（RUF001/002/003）。ASCII `()`, `:`, `+`, `=`・`->`/`→`/`・` は可。既存 Japanese prose と `—` は踏襲。
- **本ブランチは増分③（VM-side sort）④（親D&D）でパリティ復旧するまで merge しない**。増分② は review チェックポイント。
- gui-test-plan: 入力経路直結（search box）の変更 = クロスカット。honest observable = tree_groups のフィルタ結果（Layer A）・debounce の非同期適用（Layer B・setText 直後は未適用 → 事象ループで適用）・prod 実測（filter 非フリーズ）。
- **フィルタ意味論**: filter = `fl in leaf_orig.lower()`。`fl in base` は冗長（LD-14 で base は leaf orig の接頭辞ゆえ `fl in base ⟹ fl in leaf`）。`shown_count`（同 `fl in lo`）と整合。

---

### Task 1: tree_groups() がフィルタを適用する

**Files:**
- Modify: `src/valisync/gui/viewmodels/channel_browser_vm.py`
- Test: `tests/gui/test_channel_browser_vm.py`

**Interfaces:**
- Consumes: 既存 `self._prep`（`(orig, lower, unit, key)`・`lower` はフィルタ用）・`self._filter_text`。
- Produces: `tree_groups()` が `fl in lower` にマッチするリーフのみを base でグルーピング（マッチ 0 の base は現れない）。フィルタ空なら全件（現行と同じ）。base first-seen 順保持。

- [ ] **Step 1: 失敗テストを書く**

`tests/gui/test_channel_browser_vm.py` に追加:

```python
def test_tree_groups_honors_filter(tmp_path: Path) -> None:
    """FU-22 B 増分2: tree_groups は fl in leaf名 でリーフを絞り base で再グルーピング。"""
    import numpy as np

    app_vm = AppViewModel()
    vm = ChannelBrowserVM(app_vm)

    def _sig(name: str) -> Signal:
        return Signal(
            name=name, timestamps=np.array([0.0]), values=np.array([1.0]),
            file_format="MDF4", bus_type="", source_file="", metadata={"unit": "V"},
        )

    app_vm.session.group_signals = lambda k: [
        _sig("g::Arr[0]"), _sig("g::Arr[1]"), _sig("g::Speed"), _sig("g::Brake"),
    ]
    app_vm.set_active_file("g")

    # no filter -> all bases
    assert [b for b, _ in vm.tree_groups()] == ["Arr", "Speed", "Brake"]

    # filter matches a base name (prefix of its leaves) -> that base with all children
    vm.set_filter("arr")
    groups = vm.tree_groups()
    assert [b for b, _ in groups] == ["Arr"]
    assert len(groups[0][1]) == 2  # both Arr[0], Arr[1]

    # filter matches a specific leaf -> only that base, only matching leaves
    vm.set_filter("arr[1]")
    groups = vm.tree_groups()
    assert [b for b, _ in groups] == ["Arr"]
    assert [orig for orig, _u, _k in groups[0][1]] == ["Arr[1]"]

    # filter matches a scalar
    vm.set_filter("speed")
    assert [b for b, _ in vm.tree_groups()] == ["Speed"]

    # no match -> empty
    vm.set_filter("zzz")
    assert vm.tree_groups() == []
```

- [ ] **Step 2: RED 確認**

Run: `uv run pytest tests/gui/test_channel_browser_vm.py::test_tree_groups_honors_filter -v`
Expected: FAIL（現行 tree_groups はフィルタ無視で全件返す）。

- [ ] **Step 3: 実装**

`channel_browser_vm.py` の `tree_groups()` にフィルタを追加。現行はガード後に全 `_prep` をグルーピングしている。`fl` を計算し、ループ本体でマッチしないリーフを skip:

```python
    def tree_groups(self) -> list[tuple[str, list[tuple[str, str, str]]]]:
        """Group the active file's signals by base channel for the tree browser.

        Returns [(base, [(orig, unit, key), ...]), ...] in base first-seen order.
        Filtered by the current substring filter (fl in leaf name); a base appears
        only if at least one of its leaves matches. Empty filter -> all signals."""
        try:
            self._ensure_prep()
        except KeyError:
            return []
        fl = self._filter_text.lower()
        groups: dict[str, list[tuple[str, str, str]]] = {}
        order: list[str] = []
        for orig, lower, unit, key in self._prep:
            if fl and fl not in lower:
                continue
            base = _base_of(orig)
            bucket = groups.get(base)
            if bucket is None:
                bucket = groups[base] = []
                order.append(base)
            bucket.append((orig, unit, key))
        return [(b, groups[b]) for b in order]
```

（変更点: `fl = self._filter_text.lower()` 追加・ループ変数 `_lower` -> `lower` に改名して使用・`if fl and fl not in lower: continue` を先頭に。ガード/戻り値は不変。）

- [ ] **Step 4: GREEN 確認**

Run: `uv run pytest tests/gui/test_channel_browser_vm.py -v`
Expected: 新規含め全 PASS。既存 `test_tree_groups_buckets_arrays_under_base`（フィルタ空）も無回帰。

- [ ] **Step 5: 品質ゲート + コミット**

Run: `uv run pytest`; `uv run ruff check`; `uv run ruff format --check`; `uv run mypy src/`

```bash
git add src/valisync/gui/viewmodels/channel_browser_vm.py tests/gui/test_channel_browser_vm.py
git commit -m "feat(fu22b): tree_groups がフィルタ(fl in leaf名)を適用し base で再グルーピング"
```

---

### Task 2: 検索ボックスを debounce しフィルタを復旧する

**Files:**
- Modify: `src/valisync/gui/views/channel_browser_view.py`
- Test: `tests/gui/test_channel_browser_view.py`

**Interfaces:**
- Consumes: Task 1 のフィルタ対応 `tree_groups()`。`self.search_box`（QLineEdit）・`self._vm.set_filter`。
- Produces: `search_box.textChanged` は即時 `set_filter` せず、`_FILTER_DEBOUNCE_MS`（200）後に 1 回 `set_filter(現在テキスト)` する（singleShot QTimer・打鍵ごと restart）。フィルタ結果はツリー reset で反映。

- [ ] **Step 1: 失敗テスト（debounce の非同期適用）**

`tests/gui/test_channel_browser_view.py` の `TestSearchFilter` を更新。まず skip 中の `test_search_box_filters_list` を tree + debounce 対応で復活させ、debounce の非同期性テストを追加:

```python
class TestSearchFilter:
    def test_search_box_filters_list(self, qtbot: QtBot, tmp_path: Path) -> None:
        app_vm, key = _setup_app(tmp_path)  # sig_a, sig_b (scalars)
        app_vm.set_active_file(key)
        vm = ChannelBrowserVM(app_vm)
        view = _make_view(qtbot, vm)

        assert view.model.rowCount() == 2  # both scalars = top-level leaves
        view.search_box.setText("sig_a")
        # Debounced: filter applies after the timer fires in the event loop.
        qtbot.waitUntil(lambda: view.model.rowCount() == 1, timeout=1000)

    def test_search_box_debounces_filter(self, qtbot: QtBot, tmp_path: Path) -> None:
        """FU-22 B 増分2: 打鍵で即 set_filter せず debounce 後に 1 回適用。"""
        app_vm, key = _setup_app(tmp_path)
        app_vm.set_active_file(key)
        vm = ChannelBrowserVM(app_vm)
        view = _make_view(qtbot, vm)

        view.search_box.setText("sig_a")
        assert vm._filter_text == ""  # NOT applied synchronously (debounced)
        qtbot.waitUntil(lambda: vm._filter_text == "sig_a", timeout=1000)  # applied after debounce
```

（`@pytest.mark.skip` は `test_search_box_filters_list` から除去。）

- [ ] **Step 2: RED 確認**

Run: `uv run pytest tests/gui/test_channel_browser_view.py -k "search_box" -v`
Expected: `test_search_box_debounces_filter` が FAIL（現行は `textChanged` 直結で同期適用 → `vm._filter_text == ""` の assert が落ちる）。`test_search_box_filters_list` は debounce 未実装でも waitUntil で通り得るが Step 3 後に安定。

- [ ] **Step 3: 実装（debounce）**

`channel_browser_view.py`:
1. import: `from PySide6.QtCore import (...)` ブロックに `QTimer` を追加（アルファベット順は ruff format が整える）。
2. モジュール定数を追加（`_EMPTY_MESSAGES` 付近）:

```python
_FILTER_DEBOUNCE_MS = 200  # prod 264k のフィルタ scan ~170ms を入力停止後 1 回に集約
```

3. `__init__` で search_box 生成の後、debounce タイマーを作る:

```python
        self._filter_timer = QTimer(self)
        self._filter_timer.setSingleShot(True)
        self._filter_timer.setInterval(_FILTER_DEBOUNCE_MS)
        self._filter_timer.timeout.connect(self._apply_filter)
```

4. Wiring を差し替え（現行 `self.search_box.textChanged.connect(self._vm.set_filter)`）:

```python
        self.search_box.textChanged.connect(lambda _text: self._filter_timer.start())
```

5. スロット追加（`_refresh_state` 付近）:

```python
    def _apply_filter(self) -> None:
        """Apply the (debounced) search text to the VM filter."""
        self._vm.set_filter(self.search_box.text())
```

- [ ] **Step 4: 既存フィルタテストを debounce 対応へ**

`test_placeholder_no_match_includes_query_and_recovers`（現行 setText 直後に同期 assert）を waitUntil へ:

```python
def test_placeholder_no_match_includes_query_and_recovers(
    qtbot: QtBot, tmp_path: Path
) -> None:
    app_vm, vm, key = _loaded_vm(tmp_path)
    app_vm.set_active_file(key)
    view = _make_view(qtbot, vm)
    view.search_box.setText("xyz123")  # debounced textChanged -> set_filter
    qtbot.waitUntil(lambda: view.is_showing_placeholder(), timeout=1000)
    assert "xyz123" in view.placeholder_label.text()
    view.search_box.setText("")
    qtbot.waitUntil(lambda: not view.is_showing_placeholder(), timeout=1000)
```

（`tests/gui` 全体で `search_box.setText` を grep し、setText 直後に同期 assert している箇所は全て waitUntil へ。上記2つ以外に無いことを確認・報告。）

- [ ] **Step 5: GREEN + 品質ゲート**

Run: `uv run pytest tests/gui/test_channel_browser_view.py -v`（filter/debounce/placeholder 全 GREEN）
Run: `uv run pytest`; `uv run ruff check`; `uv run ruff format --check`; `uv run mypy src/`
Expected: 全通過。

- [ ] **Step 6: コミット**

```bash
git add src/valisync/gui/views/channel_browser_view.py tests/gui/test_channel_browser_view.py
git commit -m "feat(fu22b): 検索ボックスを debounce しツリーフィルタを復旧(prod scan を入力停止後1回に)"
```

---

### Task 3: prod 実測（フィルタ非フリーズの記録）= メインセッション駆動

**Files:**
- Scratch（非コミット）: `scratchpad/spike_fu22b_filter.py`（既存）を実コードで再走。

- [ ] **Step 1: prod 実測**

実 cbview で search_box 経由（debounce 込み）のフィルタを prod 264k で確認。scan ~170ms・end-to-end set_filter->reset ~250ms・debounce で per-keystroke ブロック無しを記録。数値を spec/catalog に反映（非コミット）。
Expected: フィルタが per-keystroke フリーズを起こさない（debounce で入力中ブロック無し・適用 1 回 ~250ms）。

---

## Self-Review

- **Spec coverage**: 増分②（filter=`fl in leaf名`・A 階層保持・debounce）= Task 1-3。sort③/親D&D④/磨き⑤は本プラン対象外。
- **Placeholder scan**: 全 step に実コード/実コマンド。
- **Type consistency**: `tree_groups() -> list[tuple[str, list[tuple[str,str,str]]]]`（フィルタ後も同型）・`shown_count`（同 `fl in lo`）と整合・`_apply_filter()`/`_filter_timer`/`_FILTER_DEBOUNCE_MS` は Task 2 内で一貫。debounce で `set_filter` の呼出タイミングのみ変わり `set_filter`/`notify("filter")` の契約は不変。
