# FU-22 (B) 増分③ VM-side sort 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PC-20 のヘッダクリック列ソート（Name/Unit・大小無視）を、proxy 撤去済の階層ツリーで VM-side（`SignalTreeModel` が自前でソート）に復旧する。遅延（materialized 0）を壊さない。

**Architecture:** `SignalTreeModel.sort(column, order)` が `_top`（base ノード）を即ソートし、各親の children は materialize 時に現在のソート状態で並べる（未展開の子は materialize まで作らない = 遅延保持）。ソート後は node.row を再割当（`parent()` が row で index 復元するため必須）。増分① で proxy 撤去に伴い削除した `setSortingEnabled` を view に戻す。設計 [spec](../specs/2026-07-13-fu22b-channel-tree-virtualization-design.md)。

**Tech Stack:** PySide6/pyqtgraph, MVVM, pytest-qt。

## Global Constraints

- `SignalTreeModel` は gui/adapters。core は Qt 非依存維持。
- 品質ゲート: `uv run pytest` / `uv run ruff check` / `uv run ruff format --check` / `uv run mypy src/` 全通過。
- Python コメント/文字列に全角約物 `()：+=` 禁止（RUF001/002/003）。ASCII と `->`/`→`/`・` は可。
- **本ブランチは増分④（親D&D）でパリティ復旧するまで merge しない**。増分③ は review チェックポイント。
- **最重要（遅延保持）**: sort は `_top` と**既に materialize 済**の children のみ並べる。未 materialize の親の children を sort のために作ってはならない（proxy を撤去した目的＝遅延を壊す）。prod で materialized 0 を Task 3 で実測。
- override 型: `sort(self, column: int, order: Qt.SortOrder = ...)` は QAbstractItemModel の署名に合わせる。既存 `_Index = QModelIndex | QPersistentModelIndex`・`# type: ignore[override]` パターン踏襲。
- gui-test-plan: モデルロジック（Layer A 中心・sort/row 再割当/遅延保持）+ 入力経路（ヘッダクリック = Layer B）+ prod 実測（sort が materialize を誘発しない）。

---

### Task 1: SignalTreeModel.sort() + 遅延保持ソート

**Files:**
- Modify: `src/valisync/gui/adapters/signal_tree_model.py`
- Test: `tests/gui/test_signal_tree_model.py`

**Interfaces:**
- Produces: `sort(column, order)` = `_top` を即ソート + materialize 済 children を再ソート + `_sort_col`/`_sort_order` を保存。`_materialize` が新規 children に現ソートを適用。`_rebuild` が rebuild 後に `_sort_top()` を適用（filter/signal 変更でソート維持）。`column<0` はパススルー（session 順）。ソートキーは大小無視（`.lower()`）・col0=orig・col1=unit。

- [ ] **Step 1: 失敗テスト（sort・遅延保持・row 再割当）**

`tests/gui/test_signal_tree_model.py` に追加（既存 `_model` ヘルパは Arr[0..2]+Scalar。ソート検証には非アルファベット順の複数 base が要るので専用フィクスチャを足す）:

```python
def _sort_model(qtbot: QtBot) -> SignalTreeModel:
    app_vm = AppViewModel()
    vm = ChannelBrowserVM(app_vm)
    app_vm.session.group_signals = lambda k: [
        _sig("g::Zeta"), _sig("g::alpha"), _sig("g::Mid"),
        _sig("g::Arr[2]"), _sig("g::Arr[0]"), _sig("g::Arr[1]"),
    ]
    app_vm.set_active_file("g")
    return SignalTreeModel(vm)


def _top_names(m: SignalTreeModel) -> list[str]:
    return [
        m.data(m.index(r, 0, QModelIndex()), Qt.ItemDataRole.DisplayRole)
        for r in range(m.rowCount(QModelIndex()))
    ]


def test_sort_top_level_case_insensitive_ascending(qtbot: QtBot) -> None:
    m = _sort_model(qtbot)
    # session order: Zeta, alpha, Mid, Arr (Arr is a parent grouping the 3 leaves)
    m.sort(0, Qt.SortOrder.AscendingOrder)
    # case-insensitive A-Z: alpha, Arr, Mid, Zeta
    assert _top_names(m) == ["alpha", "Arr", "Mid", "Zeta"]


def test_sort_top_level_descending(qtbot: QtBot) -> None:
    m = _sort_model(qtbot)
    m.sort(0, Qt.SortOrder.DescendingOrder)
    assert _top_names(m) == ["Zeta", "Mid", "Arr", "alpha"]


def test_sort_does_not_materialize_children(qtbot: QtBot) -> None:
    """FU-22 B 遅延保持: sort は未展開の親の children を作らない(proxy 撤去の目的)。"""
    m = _sort_model(qtbot)
    m.sort(0, Qt.SortOrder.AscendingOrder)
    parents = [n for n in m._top if n.key is None]
    assert parents and all(n.children is None for n in parents)  # materialized 0


def test_sort_row_reassigned_parent_round_trip(qtbot: QtBot) -> None:
    """sort 後も node.row が再割当され parent() 往復が成立する。"""
    m = _sort_model(qtbot)
    m.sort(0, Qt.SortOrder.AscendingOrder)
    for r in range(m.rowCount(QModelIndex())):
        idx = m.index(r, 0, QModelIndex())
        assert idx.row() == r
        assert m.parent(idx) == QModelIndex()
    # child round-trip after materialize (Arr is at sorted row 1)
    arr = m.index(1, 0, QModelIndex())
    assert m.data(arr, Qt.ItemDataRole.DisplayRole) == "Arr"
    child = m.index(0, 0, arr)
    assert m.parent(child) == arr


def test_children_sorted_on_materialize(qtbot: QtBot) -> None:
    """展開時に children が現在のソートで並ぶ(Arr[2],Arr[0],Arr[1] -> Arr[0],Arr[1],Arr[2])。"""
    m = _sort_model(qtbot)
    m.sort(0, Qt.SortOrder.AscendingOrder)
    arr = m.index(1, 0, QModelIndex())  # Arr after sort
    names = [
        m.data(m.index(r, 0, arr), Qt.ItemDataRole.DisplayRole)
        for r in range(m.rowCount(arr))
    ]
    assert names == ["Arr[0]", "Arr[1]", "Arr[2]"]


def test_sort_preserved_across_filter(qtbot: QtBot) -> None:
    """sort 状態は filter 由来の rebuild を跨いで維持される。"""
    m = _sort_model(qtbot)
    m.sort(0, Qt.SortOrder.DescendingOrder)
    m._vm.set_filter("a")  # notify 'filter' -> _rebuild
    # 'a' matches alpha and Arr[*] (Arr base is prefix). Desc order among survivors.
    names = _top_names(m)
    assert names == sorted(names, key=str.lower, reverse=True)
```

- [ ] **Step 2: RED 確認**

Run: `uv run pytest tests/gui/test_signal_tree_model.py -k "sort or materialize or round_trip" -v`
Expected: FAIL（`sort` は QAbstractItemModel 既定の no-op で `_top` が session 順のまま）。

- [ ] **Step 3: 実装**

`signal_tree_model.py`:

1. `__init__` の `self._top: list[_Node] = []` の直後（`_rebuild()` の**前**）にソート状態を追加:

```python
        self._sort_col: int = -1
        self._sort_order: Qt.SortOrder = Qt.SortOrder.AscendingOrder
```

2. `_rebuild` の末尾（`self._top = top` の後）にソート適用を追加:

```python
        self._top = top
        self._sort_top()  # preserve the active sort across filter/signal rebuilds
```

3. `_materialize` の children 構築後にソート適用:

```python
    def _materialize(self, node: _Node) -> None:
        if node.children is None:
            node.children = [
                _Node(orig, unit, key, None, node, r)
                for r, (orig, unit, key) in enumerate(node.leaves or [])
            ]
            self._sort_children(node)  # apply the active sort to freshly-built children
```

4. `columnCount` の後（navigation セクション末尾）にソートメソッド群を追加:

```python
    # --- sort (VM-side; proxy was dropped -- it materialized all children) -------
    def sort(  # type: ignore[override]
        self, column: int, order: Qt.SortOrder = Qt.SortOrder.AscendingOrder
    ) -> None:
        """Sort top-level bases now; children are sorted lazily on materialize so
        an unexpanded array is never built just to sort (preserves the lazy tree)."""
        self._sort_col = column
        self._sort_order = order
        self.beginResetModel()
        self._sort_top()
        for node in self._top:
            if node.children is not None:  # already-materialized parents only
                self._sort_children(node)
        self.endResetModel()

    def _sort_key(self, node: _Node, column: int) -> str:
        return (node.orig if column == 0 else node.unit).lower()

    def _sort_top(self) -> None:
        if self._sort_col < 0:
            return  # passthrough: keep session order (as built by _rebuild)
        reverse = self._sort_order == Qt.SortOrder.DescendingOrder
        self._top.sort(key=lambda n: self._sort_key(n, self._sort_col), reverse=reverse)
        for i, n in enumerate(self._top):
            n.row = i

    def _sort_children(self, node: _Node) -> None:
        if self._sort_col < 0 or not node.children:
            return
        reverse = self._sort_order == Qt.SortOrder.DescendingOrder
        node.children.sort(
            key=lambda n: self._sort_key(n, self._sort_col), reverse=reverse
        )
        for i, n in enumerate(node.children):
            n.row = i
```

- [ ] **Step 4: GREEN 確認**

Run: `uv run pytest tests/gui/test_signal_tree_model.py -v`
Expected: 新規含め全 PASS。既存ナビ/提示テストも無回帰。

- [ ] **Step 5: 品質ゲート + コミット**

Run: `uv run pytest`; `uv run ruff check`; `uv run ruff format --check`; `uv run mypy src/`

```bash
git add src/valisync/gui/adapters/signal_tree_model.py tests/gui/test_signal_tree_model.py
git commit -m "feat(fu22b): SignalTreeModel VM-side sort(遅延保持・row 再割当・大小無視)"
```

---

### Task 2: view で sort を再有効化 + PC-20 sort テスト復旧

**Files:**
- Modify: `src/valisync/gui/views/channel_browser_view.py`
- Test: `tests/gui/test_channel_browser_view.py`

**Interfaces:**
- Consumes: Task 1 の `SignalTreeModel.sort()`。
- Produces: `self.tree.setSortingEnabled(True)` + 既定 `sortByColumn(-1)`（session 順）。ヘッダクリックで Qt が `model.sort(column, order)` を呼ぶ。PC-20 sort テスト5本を un-skip し proxy 参照を model 直結へ。

- [ ] **Step 1: PC-20 sort テストを un-skip + proxy→model 化**

`tests/gui/test_channel_browser_view.py` の skip 中5本（`test_default_order_is_source_order`・`test_header_click_sorts_by_name`・`test_selected_keys_correct_after_sort`・`test_dnd_mime_keys_correct_after_sort`・`test_sort_is_case_insensitive`）から `@pytest.mark.skip` を除去し、`view.proxy` を `view.model` へ:
- `view.proxy.sort(0, order)` -> `view.model.sort(0, order)`
- `view.proxy.index(r, 0)` -> `view.model.index(r, 0)`（top-level は default parent）
- `view.proxy.rowCount()` -> `view.model.rowCount()`
- 選択は `view.model.index(0, 0)` を `selectionModel().select(...)`（mapToSource 不要）
- skip 理由コメント/proxy 説明ブロック（増分① で「増分③で VM-side sort」に更新した箇所）を現状（VM-side 実装済）へ更新。

- [ ] **Step 2: RED 確認**

Run: `uv run pytest tests/gui/test_channel_browser_view.py -k "sort or default_order" -v`
Expected: FAIL（view はまだ `setSortingEnabled` 無効 = ヘッダクリック経路が無い / `model.sort` は Task 1 で実装済なので `view.model.sort` 直呼びテストは通り得るが、view の設定変更前は sort 有効化テストが落ちる）。

- [ ] **Step 3: 実装（view で sort 再有効化）**

`channel_browser_view.py` の `self.tree.setModel(self.model)` の後（増分① で削除した2行を戻す）:

```python
        self.tree.setSortingEnabled(True)
        # Default: session order until a header is clicked (PC-20 DP2). -1 = passthrough.
        self.tree.sortByColumn(-1, Qt.SortOrder.AscendingOrder)
```

（`Qt` は import 済。ヘッダクリックで Qt が `self.model.sort(col, order)` を呼ぶ。）

- [ ] **Step 4: GREEN + 遅延保持の view 経路確認**

Run: `uv run pytest tests/gui/test_channel_browser_view.py -v`（sort 5本 + 既存無回帰）
Expected: 全 PASS。

さらに **setSortingEnabled が materialize を誘発しないこと**を Layer B で確認するテストを追加（proxy が materialize した教訓）:

```python
def test_sorting_enabled_does_not_materialize_children(qtbot: QtBot, tmp_path: Path) -> None:
    """FU-22 B: setSortingEnabled(True) の tree でも array 子は展開まで materialize されない。"""
    _app_vm, view, _key = _make_view_with_arrays(qtbot, tmp_path)
    view.model.sort(0, Qt.SortOrder.AscendingOrder)
    qtbot.wait(10)  # let the tree react to the sort
    parents = [n for n in view.model._top if n.key is None]
    assert parents and all(n.children is None for n in parents)
```

- [ ] **Step 5: 品質ゲート + コミット**

Run: `uv run pytest`; `uv run ruff check`; `uv run ruff format --check`; `uv run mypy src/`

```bash
git add src/valisync/gui/views/channel_browser_view.py tests/gui/test_channel_browser_view.py
git commit -m "feat(fu22b): view で VM-side sort を再有効化 + PC-20 sort テスト復旧(proxy 非依存)"
```

---

### Task 3: prod 実測（sort が materialize を誘発しない）= メインセッション駆動

**Files:**
- Scratch（非コミット）: prod repro。

- [ ] **Step 1: prod 実測**

実 cbview（setSortingEnabled 有効）で prod 264k をロードし、`model.sort(0, Asc/Desc)` および実ヘッダ相当の sort 後に **materialized=0** と sort コスト（top-level 4,264 のソート = 数 ms + reset ~80ms）を実測。
Expected: sort 後も array 親の children は未 materialize（遅延保持）・sort は非フリーズ。数値を spec/catalog に記録（非コミット）。

---

## Self-Review

- **Spec coverage**: 増分③（VM-side sort・遅延保持・row 再割当・大小無視・view 再有効化・PC-20 復旧）= Task 1-3。親D&D④/磨き⑤は本プラン対象外。
- **Placeholder scan**: 全 step に実コード/実コマンド。
- **Type consistency**: `sort(column: int, order: Qt.SortOrder)`・`_sort_top()`/`_sort_children(node)`/`_sort_key(node, column)`・`_sort_col: int`/`_sort_order: Qt.SortOrder` は Task 1 内で一貫。`node.row` 再割当は `_sort_top`/`_sort_children` 両方で必須（`parent()` が row 依存）。view の `setSortingEnabled(True)`+`sortByColumn(-1)` は増分① Task 5 で削除した2行の復活。
